// Defing the array
const myarray: string[] = []
const myarraynum: Array<number> = []  // a slightly different syntax
// adding item in array
myarray.push("abc")

console.log(myarray)

type User = {
    name: string,
    dob: Date
}

const myusers: User[] = []   // array of user object


