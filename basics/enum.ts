enum size {
    small = 200, // defined strict number insted of 1
    medium,  // by default its 2
    large = '500 kcal'  // string as value
}

const fries = size.large

// iffi style of JS code so keep it simple 